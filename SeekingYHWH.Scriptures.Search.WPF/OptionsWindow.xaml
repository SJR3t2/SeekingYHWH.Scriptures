<Window x:Class="SeekingYHWH.Scriptures.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SeekingYHWH.Scriptures"
        mc:Ignorable="d"
        Title="Options"
        ShowInTaskbar="False"
        Height="550" Width="400">
    <DockPanel LastChildFill="True">
        <ComboBox DockPanel.Dock="Top" x:Name="languagesCombo" SelectionChanged="OnLanguagesChanged"/>
        <Grid DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <RadioButton Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" Content="Sentence" x:Name="portionSentence" Checked="OnPortionSentenceChecked"/>
            <RadioButton Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" Content="Verse" x:Name="portionVerse" Checked="OnPortionVerseChecked"/>
            <RadioButton Grid.Row="0" Grid.Column="3" HorizontalAlignment="Center" Content="Chapter" x:Name="portionChapter" Checked="OnPortionChapterChecked"/>
        </Grid>
        <DataGrid AutoGenerateColumns="False" x:Name="booksList">
            <DataGrid.Columns>
               <DataGridTextColumn Header="Name" Width="*" Binding="{Binding Name, Mode=OneWay}"/>
				   <DataGridCheckBoxColumn Header="Include" Binding="{Binding Include, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
				   <DataGridTemplateColumn Header="Pre">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
							      <CheckBox IsEnabled="{Binding PrePossible, Mode=OneWay}" IsChecked="{Binding PreUse, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
			</DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
